 <section id="registration-section" class="devise-user-section">
  <div class="container devise-user-container text-center">
    <h2>Sign up</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="field row">
        <%= f.label :email, class: 'col devise-user-labels' %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'devise-user-inputs col' %>
      </div>

      <div class="field row">
        <div class="col devise-user-labels">
          <%= f.label :password %>
          <em>(Min 6 chars)</em>
        </div>
        <%= f.password_field :password, autocomplete: "new-password", class: 'devise-user-inputs col' %>
      </div>

      <div class="field row">
        <%= f.label :password_confirmation, class: 'devise-user-labels col' %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'devise-user-inputs col' %>
      </div>

      <div class="row devise-user-buttons">
        <a class="col" href="/users/sign_in">Already have an account?</a>
        <div class="actions col">
          <%= f.submit "Sign up" %>
        </div>
      </div>
    <% end %>
    
  </div>
</section>
